# Overview

`packages/ui/src`

## Purpose

Implementation and public surface of `@multi-coc/ui`. `index.ts` is the package boundary; internal files remain grouped by
component responsibility and styling role.

## Subfolders

- `components/` — accessible primitives and reusable compositions.
- `lib/` — class-name and component utilities.
- `styles/` — tokens, foundations, and component presentation.

## Key files

- `index.ts` — explicitly exported public API.

