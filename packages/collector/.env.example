# Collector를 컨테이너 없이 단독 실행할 때만 .env로 복사합니다.
ADMIN_TOKEN=replace-with-a-long-random-admin-token
DATABASE_URL=postgresql://coc:replace-with-database-password@127.0.0.1:5432/multi_coc
DATA_DIR=../../data
PORT=8787
CORS_ORIGIN=http://localhost:3000

# 계정별 토큰 대신 공용 공식 API 토큰을 쓸 때만 설정합니다.
# CLASH_OF_CLANS_API_TOKEN=official-developer-api-token

POLL_INTERVAL_SECONDS=300
INGEST_RATE_LIMIT_PER_MINUTE=120
SNAPSHOT_RETENTION_DAYS=90
EVENT_RETENTION_DAYS=90
