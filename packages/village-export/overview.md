# Overview

`packages/village-export`

## Purpose

Pure parser and validator for pasted Clash of Clans export JSON. It normalizes player identity, active upgrades, slots,
cooldowns, helpers, Hero Equipment, stable upgrade keys, and preview diffs.

`index.ts` is the public API; tests encode supported export facts and rejection safeguards.

