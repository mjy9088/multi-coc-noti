# Overview

`packages/maintenance`

## Purpose

Operator CLI package for local test-user village-history backup, restore, seed, and reseed. It composes Database history
operations with village-export validation and is not part of the browser-facing API.

## Subfolders

- `src/` — JSONL history command implementation.

Use the root `just data ...` recipes instead of invoking package internals directly.

