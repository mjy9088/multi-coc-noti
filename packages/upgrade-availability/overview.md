# Overview

`packages/upgrade-availability`

## Purpose

Pure display projection for upgrade availability. It infers optional Goblin slots, filters villages, summarizes Home and
Builder Base capacity, and produces completion/active-slot chart series.

`index.ts` is the public API; tests protect slot inference, filtering, summaries, and chart projections.

